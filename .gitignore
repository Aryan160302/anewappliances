# dependencies
node_modules/

# next.js build output
.next/
out/
*.tsbuildinfo
next-env.d.ts

# vercel
.vercel

# env files
.env*

# macOS
.DS_Store

# Obsidian per-machine state
.obsidian/workspace.json

# Third-party personal data — these two images show company business cards
# carrying individual staff mobile numbers and personal email addresses. This
# repository is public, so they are kept out of it and out of its history.
# The site publishes only the company address and info@anewappliances.com.
# Keep them locally; scripts/extract_assets.py uses them when present.
data/PHOTO-2026-07-20-18-16-52.jpg
data/logo.png

# Commercially sensitive — this workbook's "Induction Cooktop Plus",
# "Folderbable Double Hob IC" and "Induction Stove Slim Model" sheets are
# costings: landed import price per unit, wastage, transport, labour and the
# margin applied. Only the product descriptions on its first sheet are used by
# the site, and those are transcribed into src/data/products.ts. This
# repository is public, so the workbook itself stays out of it.
# Keep it locally; scripts/extract_assets.py uses it when present.
data/new data/ICT Premium Models.xlsx
